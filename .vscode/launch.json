{
    "configurations": [
        {
            "type": "firefox",
            "name": "localhost:3000",
            "request": "launch",
            "reAttach": true,
            "url": "localhost:3000",
            "webRoot": "${workspaceFolder}",
            "pathMappings": [
                {
                    "url": "webpack://_n_e",
                    "path": "${workspaceFolder}"
                }
            ]
        }
    ]
}
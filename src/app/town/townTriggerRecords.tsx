import { triggerType } from "../hooks/useTriggers";

export const townTriggerRecord: Record<string, triggerType> = {
  wgmnklq: { type: "redirect", route: `/opening?prev=town` },
  rsn336m: { type: "conversation", key: "introduction" },
};
